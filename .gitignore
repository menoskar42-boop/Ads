node_modules
.env
*.log

# Kakeibo receipts and anything else deliberately outside the web root.
private_uploads/
backups/ads/

# Safari Kids (mykid) runtime artifacts
mykid/server/.tts-cache/
mykid/.env
