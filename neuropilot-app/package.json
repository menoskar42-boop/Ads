{
  "name": "neuropilot-app",
  "version": "1.0.0",
  "private": true,
  "description": "NeuroPilot native shell — background geofencing for the ADHD focus app",
  "scripts": {
    "web": "node scripts/copy-web.js",
    "overlay": "node scripts/apply-overlay.js",
    "add:android": "npx cap add android",
    "add:ios": "npx cap add ios",
    "sync": "npm run web && npx cap sync && npm run overlay && npm run gradle",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease",
    "gradle": "node scripts/patch-gradle.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
