<%#
  Every entry point a merchant panel offers, in one place. Panels include THIS,
  so the next tool to be surfaced is one edit here rather than eleven.
%>
<span class="inline-flex items-center gap-2 flex-wrap">
  <%- include('./pay_link') %>
  <%- include('./einvoice_link') %>
</span>
