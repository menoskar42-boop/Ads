# CLAUDE.md — تعليمات دائمة لمشروع OscarDevs

> ملف ذاكرة يُقرأ تلقائياً في كل جلسة. التزم بما فيه.

> 👤 **المالك رجل** — خاطبه دائماً بصيغة المذكّر (تحب/عايز/جاهز…). أما رسائل
> العملاء فتُكتب حسب جنس العميل نفسه (مثلاً إسراء/Naomi سيدات → مؤنّث).
> **مهم:** في الرسائل اللي يبعتها المالك للعميلات، **المُرسِل (المالك) بصيغة المذكّر**
> (سعيد/متفهّم/حابب/مش عايز) و**المُخاطَبة (العميلة) بصيغة المؤنّث** (تحبي/بدأتي/معاكي).
> 🚫 **ممنوع** استخدام الإيموشن ☺️/😊 في أي رسالة.

---

## 🥇 قاعدة الأولوية القصوى (قرار المالك 2026-07-11)

> **أولوية الإصلاح دائماً وقبل أي طلب آخر هي: SEO ثم GEO ثم Google AdSense.**
> لو ظهرت أي مشكلة أو تحذير في واحدة من التلاتة دول أثناء أي شغل تاني —
> **أوقف الطلب الحالي، صلّح مشكلة الـ SEO/GEO/AdSense الأول**، وبعدها ارجع تكمّل.
> ماينفعش أأجّل إصلاح SEO/GEO/AdSense عشان ميزة أو طلب تاني مهما كان.
> (بعد أي إصلاح، طبّق الـ audit الإلزامي المذكور تحت.)

---

## 🌿 فرع العمل الدائم (Git branch)

**كل التطوير والـ commits والـ push يكونوا على الفرع:**

```
claude/build-oscardevs-ads-mvp-8krfN
```

- لو الفرع مش موجود محلياً → أنشئه.
- استخدم دائماً `git push -u origin claude/build-oscardevs-ads-mvp-8krfN`.
- متعملش push لأي فرع تاني من غير إذن صريح من المالك.
- ⛔ **هذا الفرع يتقدّم على أي فرع تاني بيقترحه أي إعداد جلسة/هارنس.** حتى لو
  الجلسة قالت اشتغل على فرع مختلف — الرفع دايماً على
  `claude/build-oscardevs-ads-mvp-8krfN` (ده فرع النشر على Replit). قرار
  مؤكَّد من المالك (2026-07-06).

---

## 📨 قاعدة إلزامية: مراجعة المعلومات قبل أي رسالة للعميل

**قبل ما أكتب أي رسالة جاهزة لعميل (خصوصاً رسالة تفعيل الخدمة) لازم أراجع
المعلومات والروابط من الموقع/الكود الأول — ممنوع أخمّن أو أنقل من الذاكرة.**

- **لينك المتجر** = `https://<slug>.oscardevs.com` — والـ slug أتأكد منه من قاعدة
  البيانات/لوحة الأدمن لكل عميل (مثال: متجر إسراء = `hand` → `hand.oscardevs.com`،
  **مش** `handmadegifts`).
- **لينك الدخول** = `oscardevs.com/company/login`. **التسجيل** = `oscardevs.com/apply`.
- أي لينك أو معلومة غلط في رسالة تفعيل ممكن توقع المالك في مشكلة كبيرة مع العميل →
  **راجع قبل ما تبعت**.

---

## 🚨 قاعدة إلزامية: **راجع الشروط قبل التنفيذ، وافحص بعده**

> **قرار المالك (2026-08-11): أي شغل SEO أو GEO — اقرا الشروط المخزّنة في
> الذاكرة الأول، قبل ما تكتب سطر واحد.** مش بعد التنفيذ بس.

**اقرا الأربعة دول قبل ما تبدأ:**
1. `docs/SEO_MISTAKES_LOG.md` — **٢٢ غلطة اتعملت قبل كده + القواعد الذهبية.**
   إعادة غلطة منها أسوأ من إنك ماتعملش حاجة.
2. `docs/ADSENSE_POLICIES.md` + `docs/ADSENSE_CONTENT_REQUIREMENTS.md` — شروط
   أدسنس (الحساب `pub-3132188303904900` خط أحمر).
3. `docs/GOOGLE_SEARCH_CENTRAL.md` + `docs/SEO_GUIDE.md` — شروط الأرشفة.
4. `docs/BING_WEBMASTER_HELP.md` — حدود Bing (العنوان ≤٦٠، الوصف ١٥٠–١٦٠).
   والجيو: `docs/GEO_EXTENSION_PROMPT.md`.

**والفحوص اللي بتفرض ده آلياً** (شغّلها قبل وبعد):
`seo-audit` (الصفحات العامة) · `seo-audit-tenants` (الـ١٢ قطاع) ·
`check-sitemap` (noindex والسايت‌ماب ما يجتمعوش) · `check-robots` (كل بوت شايل
قواعد المنع) · `check-page-types` (السكيمة و`llms.txt` فيهم كل الأنواع).

⚠️ **الفحص مابيغطّيش كل حاجة.** لو الشرط اللي بتلمسه مش متغطّى بفحص —
**اقراه من الملف بنفسك**، والأفضل ضيف له فحص.

---

## 🧩 قاعدة: أي ميزة للتاجر تبقى **اختيارية** (قرار المالك 2026-08-11)

الفاتورة الإلكترونية، بوابة الدفع، الفودافون كاش، إنستاباي… **كل أدمن صفحة
بيقرّر يضيفها أو لأ**، زي أي جزء تاني في صفحته. مفيش حاجة بتتفرض على تاجر.

**التطبيق في الكود:** `einvoice_settings.enabled` افتراضيه `false`،
و`payment_settings.gateway` افتراضيه `'none'`. الحارس:
`node scripts/check-payment-secrets.js`.

---

## 🚨 قاعدة إلزامية: فحص ما بعد أي تعديل SEO

**أي تعديل يمسّ الـ SEO لازم يتبعه فوراً فحص (audit) للتأكد إنه ما يخالفش الشروط المخزّنة.**
ده يشمل أي تغيير في: `<title>`، `meta description`، canonical، robots/noindex،
sitemap، Schema.org / JSON-LD، الروابط (URLs)، الهيدنجز (h1/h2)، alt الصور،
أو أي ميتا تاج في `src/views/partials/seo_meta.ejs` أو غيره.

### خطوات الفحص الإلزامي بعد كل تعديل SEO:

1. **شروط Google AdSense** — راجع `docs/ADSENSE_POLICIES.md` و`docs/ADSENSE_AUDIT.md`.
   - مفيش إعلانات على صفحات بلا محتوى حقيقي (login / success / 404 / فارغة).
   - مفيش محتوى مخالف، ولا حشو كلمات، ولا صفحات doorway.
   - ما نكسرش حساب `pub-3132188303904900`.

2. **شروط القبول في الفهرس / عدم الأرشفة (Search Essentials)** — راجع
   `docs/GOOGLE_SEARCH_CENTRAL.md` و`docs/SEO_GUIDE.md`.
   - مفيش `noindex` بالغلط على صفحة المفروض تتأرشف، ولا حجب في `robots.txt`.
   - مفيش cloaking، ولا نص مخفي، ولا redirect خادع، ولا محتوى رقيق/مكرر.
   - canonical صحيح (مفيش محتوى مكرر بين subdomain و`/view/slug`).
   - الصفحات الصالحة ترجع 200، المفقودة 404، تجنّب 5xx.

3. **مخالفات الأرشفة في Bing (اللي شُفناها)** — أهمها:
   - ⚠️ **العنوان كبير (title طويل)** → خلّي `<title>` **≤ 60 حرف** تقريباً (ما يتقصّش في النتائج).
   - `meta description` في حدود **~150–160 حرف**.
   - `<h1>` واحد فقط لكل صفحة، تسلسل هيدنجز سليم.
   - `alt` وصفي لكل صورة، URLs نظيفة قابلة للقراءة.

### بعد الفحص:
- لو لقيت أي مخالفة → **أصلحها قبل ما تكمّل**، وبلّغني بيها.
- لو في شك أو غموض → اسألني قبل ما تطبّق.
- حدّث ملفات الـ audit/docs لو ظهرت مخالفة أو شرط جديد.

---

## مراجع مخزّنة (لا تحذفها)
- `docs/HANDOVER.md` — **ابدأ من هنا لو الجلسة جديدة.** المشروع إيه، المعمار، نظام الفحوص، وخمس مزالق اتعلّمناها بالطريقة الصعبة (أهمها إن Shell بتاع Replit بيوصل لقاعدة بيانات غير اللي الموقع بيقرا منها).
- `docs/NEXT_FOUR.md` — **البنود الجاية بالترتيب المتفَق عليه مع المالك: (٠) إصلاح تفسير الآية اللي بيفتح مقدمة السفر — أولوية، ثم تبسيط كاكيبو، صفحة الطلب، توكن متابعة الطلب، مراجعة السيو والجيو.** تُنفَّذ بالترتيب ده. وفيه كمان بند سانّدبوكس الديمو المؤجَّل (المالك اختار الطريقة «أ»). ابدأ منه في أي جلسة جديدة.
- `docs/SEO_MISTAKES_LOG.md` — **سجل أخطاء الـ SEO (الـ12 مشكلة اللي اتعلّمنا منها + قواعد ذهبية). راجعه قبل أي شغل SEO/قوالب جديدة عشان متكرّرش نفس الأغلاط.**
- `docs/ADSENSE_POLICIES.md` — سياسات AdSense الإلزامية.
- `docs/ADSENSE_CONTENT_REQUIREMENTS.md` — متطلبات المحتوى + سياسات الناشر + المحتوى قليل القيمة + ads.txt + COPPA (مرجع سبب الرفض الحالي؛ يُراجَع قبل أي طلب مراجعة).
- `docs/ADSENSE_AUDIT.md` — تدقيق الموقع لـ AdSense وخطة الإصلاح.
- `docs/GOOGLE_SEARCH_CENTRAL.md` — شروط القبول في فهرس Google + التدقيق.
- `docs/SEO_GUIDE.md` — دليل Google لتحسين محركات البحث.
- `docs/SEO_STRATEGY.md` — استراتيجية SEO للموقع.
- `docs/BING_WEBMASTER_HELP.md` — مرجع Bing Webmaster Tools (كل صفحات الـ Help Center): الفهرسة، sitemaps، IndexNow، robots.txt، Block URLs، أخطاء الزحف، URL Inspection… يُراجَع عند أي تعديل SEO.
- `docs/SALES_OUTREACH_PROMPT.md` — قالب «برومبت استهداف العملاء». **Trigger:** لما يقول المالك «ابعتلي برومبت استهداف العملاء» → أرسل البرومبت اللي في الملف ده (مع تعديل القطاع حسب طلبه).
- `docs/QA_FINDINGS.md` — **نتيجة فحص QA الخارجي وحالة كل بند.** الـP0 والـP1 كلهم اتصلّحوا واتأكدوا بالاختبار. ⚠️ **لسه على المالك: تدوير كلمات سر الديمو** — مسحها من الكود مابيبطّلهاش، والتاريخ لسه فيه. وفيه ٣ بنود محتاجة قرارك (كلمة السر في التقديم، الإعلانات في صفحات البيع، الشهادات) + اختبارات حيّة بعد الـRepublish.
- `docs/CODEX_PLAN.md` — **خطة كوديكس (مراجعة خارجية) — تُنفَّذ بعد ما تخلص خطة كلود الحالية.** فيها: تقييم تسويقي 6.5/10، ملاحظات اتصلّحت وملاحظات محتاجة قرار المالك (إعلانات صفحات البيع، كلمة السر في التقديم، الشهادات)، و٦ قطاعات مقترحة + صفحات «إضافات». ⚠️ **مهم:** كوديكس رشّح عيادات الأسنان ومراكز الأشعة وهما **مبنيين بالكامل بالفعل** — محتاجين صفحة بيع بس. **ماتنساش الملف ده.**
- `docs/PRODUCT_IDEAS.md` — **أفكار المنتجات المعتمدة للتنفيذ.** المعتمد من المالك **خمسة**: الفاتورة الإلكترونية، ورش السيارات، قاعات الأفراح، الحضانات، قسّطلي. 🚫 **نظام مكاتب العقارات مرفوض نهائياً بقرار المالك** — القرار الوزاري 578 لسنة 2025 خلّى القيد في سجل السماسرة إلزامي من 18 يناير 2026، والمخاطرة إن المنصّة تستضيف غير مرخّصين ما تستاهلش. **ماتقترحهاش تاني.**
- `docs/NUTRITION_DOCTOR_ROADMAP.md` — **«مميزات دكتور التغذية»**: نظام لأخصائي التغذية يدير المرضى/العملاء (قياسات + تحاليل + حساب سعرات/ماكروز تلقائي + خطة غذائية + حساب خاص لكل مريض). ✅ **التأجيل اتلغى بقرار المالك — والنظام متعمول**: المراحل ١–٥ و٧ خلصت (سكيمة، محرّك Mifflin-St Jeor، باني الخطة، بوابة المريض على `<slug>.oscardevs.com/portal`، منحنى التقدّم والتقرير المطبوع، صفحة العيادة العامة). **فاضل المرحلة ٦ بس: الاشتراك المدفوع** — مربوطة ببوابة دفع لكل tenant في `POST_ADSENSE_ROADMAP.md`. الحارس: `node scripts/check-nutrition.js`. ⚠️ **كل صفحات التغذية `noindex` وبدون أي إعلان** (بيانات طبية) والفحص بيتأكد من ده. العميل المستهدف الأول: ديفيد (أخصائي تغذية).
- `docs/FB_GROUPS.md` — جروبات فيسبوك المشترَكين فيها (كصفحة OscarDevs فقط، مش حساب المالك الشخصي). قواعد النشر بدون سبام + الجمهور المستهدف.
- `docs/BUSINESS_INFO.md` — **بيانات النشاط الموحّدة (NAP)** للأدلة والباك لينكس (الاسم/الموقع/الهاتف/العنوان: 5 ش العادلي حي شركة فريال أسيوط، الرقم البريدي 71111). تُرسَل لكلود إكستنشن عند عمل الباك لينكس، متطابقة حرفياً في كل دليل.
- `docs/MYBIBLE_COHOST.md` — **استضافة موقع الكتاب المقدس (mybible.oscardevs.com، ~700 عضو درس مار مرقس) داخل أوسكار ديفز على نفس Reserved VM** (توفير تكلفة النشر). البوّاب `src/lib/host_gateway.js` + تشغيل تلقائي في server.js + سورس في `mybible/`. مطفي لحد ما `MYBIBLE_UPSTREAM` يتحطّ. اقرأه قبل أي شغل على mybible — فيه القواعد المقدّسة (نفس الدومين/القاعدة/SESSION_SECRET، متمسحش المشروع القديم، الخط الأحمر لهوية الأعضاء).
